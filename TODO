* documentation
* put current grammar on wiki. shouldn't need to change much now
* unit testing (esp. of parsing)
* error reporting?
* maybe print environment in comments at the top of the generated file.
* make it work on OS X
* pass through binary files unchanged.
